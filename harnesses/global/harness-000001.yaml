# harness-000001: claude-code driven by a compiled context-runtime package.
#
# First managed policy, child of the unmanaged defaults: the session starts
# from a deterministic compiled package (task-declared manifest, memory
# compiled in) and cannot close without the contract's declared checks.
# Everything else (runner loop, tools, generation) stays the runner's own.
schema_version: 1
harness_id: harness-000001
content_hash: sha256:f9624b222dab30d6356492c34b87ae7673856df36bd564e0d2c58edbcdd07e0d
parent_harness_id: harness-000000
created_at: "2026-07-23T12:00:00Z"
status: active
scope:
  task_types: []
  models: []
  repositories: []
dimensions:
  context:
    manifest_policy: task_declared_package
    max_context_tokens: 16000
    compression: true
  tools:
    allowed: [shell, file_read, file_edit, search, web, task_agents]
    retrieval_top_k: 0
    retry_policy: model_discretion
  generation:
    temperature: 1.0
    max_output_tokens: 32000
  orchestration:
    workflow: agentic_loop
    max_steps: 1000
  memory:
    retrieve_successes: 0
    retrieve_failures: 0
    include_global_patterns: false
  output:
    require_schema: false
    require_declared_checks: true
    fallback: human_review
